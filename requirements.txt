fastapi==0.115.2        # FastAPI web framework
uvicorn[standard]==0.32.0  # ASGI server for FastAPI
python-multipart==0.0.6  # To handle form data (like file uploads and forms)
aiofiles==24.1.0         # For async file operations
jinja2==3.1.2            # Templating engine for HTML rendering
pydantic==2.9.2          # Data validation and parsing library
MarkupSafe==2.1.3        # Dependency for Jinja2
anyio==4.6.2             # Required for async support in FastAPI
requests==2.31.0         # For HTTP requests (if needed in your app)
python-dotenv==1.0.0     # To manage environment variables (for config)
