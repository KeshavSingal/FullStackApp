# Core FastAPI dependencies
fastapi==0.115.2          # Update version to match the latest stable or your requirement.
uvicorn[standard]==0.32.0 # ASGI server required to run FastAPI applications.

# File and form handling
python-multipart==0.0.6   # For handling form fields and file uploads in FastAPI.
aiofiles==24.1.0          # Async file system operations support.

# Templating with Jinja2
jinja2==3.1.2             # Template engine used with FastAPI.
MarkupSafe==2.1.3         # Required for Jinja2 template security.

# Pydantic for data validation
pydantic==2.9.2           # Data parsing and validation, used by FastAPI.

# Additional core dependencies for async and requests
anyio==4.6.2              # Provides async capabilities needed by FastAPI.
requests==2.31.0          # HTTP library for external requests (like the mail service).

# MongoDB dependencies
pymongo==4.6.1            # MongoDB driver required for database operations.

# Environment variable handling
python-dotenv==1.0.0      # Used for managing environment variables securely.

# Email validation for user registration
email-validator==2.1.0.post1  # Validates email formats.

# Authentication and encryption
python-jose[cryptography]==3.3.0 # For handling secure tokens and cryptographic functions.
passlib[bcrypt]==1.7.4           # Password hashing library, recommended for security.

# Any other dependencies not specified but required by your project
